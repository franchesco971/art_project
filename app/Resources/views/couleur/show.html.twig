{% extends 'base.html.twig' %}

{% block body %}
    <h1>Couleur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Major</th>
                <td>{{ couleur.major }}</td>
            </tr>
            <tr>
                <th>Proxy</th>
                <td>{{ couleur.proxy }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_couleur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_couleur_edit', { 'id': couleur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
